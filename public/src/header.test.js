// header.js no longer exports pure helpers (the Best pill and its
// formatting were removed when the header was simplified to brand + races
// only). Tests can be added back if helpers are reintroduced.
import { test } from "node:test";

test("placeholder — header pure-helper tests deferred", () => {});
